initialState = {
    mongo_id: null,
    spotify_id: null,
    user_name: null,
    profile_picture: null,
    user_gigs: null
}

// Profile reducer
export default (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };